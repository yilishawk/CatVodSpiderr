name: Build Standard custom_spider.jar

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'

    - name: Build and Generate JAR
      run: |
        chmod +x gradlew
        ./gradlew buildAndGenJar --no-daemon --stacktrace

    - name: Verify JAR
      run: |
        if [ ! -f "jar/custom_spider.jar" ]; then
          echo "JAR 生成失败！"
          ls -la jar/ || true
          exit 1
        fi
        echo "标准 custom_spider.jar 生成成功！"

    - name: Generate MD5
      run: |
        echo "$(openssl md5 -hex jar/custom_spider.jar | awk '{print $2}')" > jar/custom_spider.jar.md5

    - name: Upload JAR
      uses: actions/upload-artifact@v4
      with:
        name: custom_spider.jar
        path: jar/custom_spider.jar

    - name: Commit & Push
      uses: EndBug/add-and-commit@v9
      with:
        message: 'update standard spider jar'
        add: 'jar/custom_spider.jar jar/custom_spider.jar.md5'
        push: true
